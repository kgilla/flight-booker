<!DOCTYPE html>
<html>
  <head>
    <title>Fly Away</title>
    <%= render 'layouts/defaults' %>
  </head>
  <body>
    <%= render "/partials/header"%>
    <div id="main">
      <%= yield %>
    </div>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
